// src/lib/featureFlags.ts

// Toggle UI capabilities here (easy to revert later if needed)
export const FLAGS = {
  /** Remove the 3 upload/drop cards on all Reports pages */
  showReportUploadCards: false,
};
